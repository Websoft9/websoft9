name: apphub Build And Push To DockerHub

on:
  push:
    branches: [main]
    paths:
      - "docker/w9apphub/Dockerfile"

jobs:
  build:
    name: Build Docker image
    runs-on: ubuntu-latest
    env:
      TAGS: latest
    steps:
      - uses: actions/checkout@v2
        name: Check out code

      - name: Get version
        run: |
          version=$(grep -Po '(?<=LABEL version=").*?(?=")' docker/w9apphub/Dockerfile)
          echo $version
          echo "::set-output name=version::$version"
          echo "VERSION=$version" >> $GITHUB_ENV

      - uses: mr-smithers-excellent/docker-build-push@v5
        name: Build & push Docker image
        with:
          image: websoft9dev/apphub
          tags: ${{ env.VERSION }}
          addLatest: True
          registry: docker.io
          dockerfile: docker/w9apphub/Dockerfile
          directory: docker/w9apphub
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

