# Story 7.2: TanStack Router Setup

**Epic**: Epic 7 - Dashboard Unified Frontend Framework  
**Priority**: P0  
**Status**: Backlog  
**Dependencies**: Story 7.1 (Project Setup)

## User Story
As a developer, I want TanStack Router with file-based routing configured, so that I can build type-safe routes with automatic route tree generation and eliminate manual route configuration.

## Acceptance Criteria
- [ ] Install `@tanstack/react-router` and `@tanstack/router-vite-plugin`
- [ ] Install `@tanstack/router-devtools` for development
- [ ] Configure Vite plugin in `vite.config.ts` for automatic route generation
- [ ] Create `src/routes/__root.tsx` with root layout
- [ ] Create basic route structure:
  - [ ] `src/routes/index.tsx` - Home/landing page
  - [ ] `src/routes/_app.tsx` - App layout wrapper
  - [ ] `src/routes/_app/_auth.tsx` - Protected routes layout
  - [ ] `src/routes/_app/login.tsx` - Login page
- [ ] Configure TypeScript for generated route types
- [ ] Replace default App.tsx routing with TanStack Router
- [ ] Router DevTools accessible in development mode
- [ ] Type-safe navigation working (`useNavigate`, `Link` components)

## Definition of Done
- [ ] File-based routes automatically generate route tree
- [ ] `routeTree.gen.ts` file auto-generated by Vite plugin
- [ ] TypeScript autocomplete works for route paths
- [ ] Navigation between routes works without errors
- [ ] DevTools visible in bottom-right corner (dev mode only)
- [ ] No console warnings related to routing
- [ ] Browser tested: navigation, back/forward buttons work

---

## Implementation Steps

### 1. Install Dependencies
```bash
npm install @tanstack/react-router
npm install -D @tanstack/router-vite-plugin @tanstack/router-devtools
```

### 2. Configure Vite
Add `TanStackRouterVite()` plugin to `vite.config.ts`

### 3. Create Route Files
```
src/routes/
├── __root.tsx              # Root with Outlet + DevTools
├── index.tsx               # Home (/)
├── _app.tsx                # App layout
└── _app/
    ├── _auth.tsx           # Protected layout
    ├── _auth/dashboard.tsx # /dashboard
    └── login.tsx           # /login
```

### 4. Update Entry Point
Replace `App.tsx` routing in `main.tsx` with `RouterProvider` + auto-generated `routeTree`

### 5. Configure TypeScript
Include `src/routeTree.gen.ts` in `tsconfig.app.json`

---

## Route Naming Convention
- `_prefix` → Layout route (no path segment)
- `index.tsx` → Default route (/)
- `$param.tsx` → Dynamic route (:param)

---

## Verification Checklist
- [ ] Dev server starts without errors
- [ ] Vite generates `src/routeTree.gen.ts` on save
- [ ] TypeScript shows route autocomplete in IDE
- [ ] Navigate to `/` shows home page
- [ ] Navigate to `/login` shows login page
- [ ] DevTools panel appears in dev mode
- [ ] Hot reload works when editing routes
- [ ] Build succeeds: `npm run build`

---

## Technical Notes

**Key Features**: Type-safe routing, file-based routes, auto-generated route tree, smaller bundle than React Router v6

**Generated Files** (do not edit): `src/routeTree.gen.ts`, `.tanstack-router/` (add to .gitignore)

---

## Next Story
**Story 7.3**: Design System Foundation - shadcn/ui, Tailwind CSS, Dark/Light theme
