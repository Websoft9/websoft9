/**
 * Global UI upload / file size limits.
 * Adjust here to apply the change across all upload surfaces.
 */

/** Maximum allowed size (in bytes) for any file imported or uploaded through the UI. Default: 1 MB */
export const MAX_FILE_SIZE = 1 * 1024 * 1024

/** Human-readable label for MAX_FILE_SIZE, used in error messages. */
export const MAX_FILE_SIZE_LABEL = '1 MB'
